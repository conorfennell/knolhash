services:
  knolhash:
    build:
      context: .
      args:
        - GIT_COMMIT=${GIT_COMMIT:-dev}
        - BUILD_DATE=${BUILD_DATE:-unknown}
    restart: unless-stopped
    volumes:
      - knolhash-data:/app/data

volumes:
  knolhash-data:
